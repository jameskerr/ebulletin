<!DOCTYPE html>
<html>
	<head>
		<title>Ebulletin</title>
		<%= render 'shared/style' %>
		<%= csrf_meta_tags %>
	</head>
	<body id="preview">
		<%= render 'shared/menu' %>
		<% if notice %>
		<p class="notice"><%= notice %></p>
		<% end %>
		<% if alert %>
		<p class="alert"><%= alert %></p>
		<% end %>
		<%= yield %>
		<%= javascript_include_tag "application" %>
	</body>
</html>
